name: CI

on:
  push:
    branches: ["main", "session/*"]
  pull_request:
    branches: ["main"]

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout conductor
        uses: actions/checkout@v4
        with:
          path: conductor

      - name: Checkout guardian (replace dep)
        uses: actions/checkout@v4
        with:
          repository: ontai-dev/guardian
          path: guardian

      - name: Checkout platform (replace dep)
        uses: actions/checkout@v4
        with:
          repository: ontai-dev/platform
          path: platform

      - name: Checkout seam-core (replace dep)
        uses: actions/checkout@v4
        with:
          repository: ontai-dev/seam-core
          path: seam-core

      - name: Checkout wrapper (replace dep)
        uses: actions/checkout@v4
        with:
          repository: ontai-dev/wrapper
          path: wrapper

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: conductor/go.mod
          cache: true

      - name: Build
        working-directory: conductor
        run: make build

      - name: Lint
        working-directory: conductor
        run: |
          go install github.com/golangci/golangci-lint/cmd/golangci-lint@latest
          golangci-lint run --timeout 5m ./...

      - name: Unit tests
        working-directory: conductor
        run: make test-unit

      - name: Install envtest binaries
        working-directory: conductor
        run: |
          go install sigs.k8s.io/controller-runtime/tools/setup-envtest@latest
          ENVTEST_BIN=$(setup-envtest use --bin-dir /tmp/envtest-bins --print path 1.35.0)
          echo "KUBEBUILDER_ASSETS=${ENVTEST_BIN}" >> "$GITHUB_ENV"

      - name: Integration tests
        working-directory: conductor
        run: make test-integration

      - name: e2e tests
        working-directory: conductor
        run: |
          E2E_OUT=$(go test ./test/e2e/... -v 2>&1 || true)
          SKIPPED=$(echo "$E2E_OUT" | grep -c '\[SKIPPED\]\|S\[' || echo 0)
          echo "### e2e summary" >> "$GITHUB_STEP_SUMMARY"
          echo "Skipped specs (MGMT_KUBECONFIG not set): ${SKIPPED}" >> "$GITHUB_STEP_SUMMARY"

      - name: Upload test output on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-output
          path: /tmp/test-*.log
          if-no-files-found: ignore
